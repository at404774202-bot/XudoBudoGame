# 🔒 Система обязательной подписки - Настройка

## Обзор системы

Реализована система обязательной подписки с следующими возможностями:

- ✅ **Проверка при запуске** - автоматическая проверка подписки через Telegram Bot API
- ✅ **Блокировка UI** - полная блокировка функционала для неподписанных пользователей  
- ✅ **Периодическая проверка** - автоматическая проверка каждые 2 минуты
- ✅ **Админ бот** - управление каналами через команды в Telegram
- ✅ **Мгновенная блокировка** - при отписке доступ блокируется немедленно

## 🛠 Настройка

### 1. Получение ID каналов

Для работы системы нужны **числовые ID каналов** (не @username):

```javascript
// В script.js найдите и обновите:
requiredChannels: [
    {
        username: '@xudobudo_news',
        name: 'XudoBudo News',
        avatar: '📢',
        id: -1001234567890 // ← ЗАМЕНИТЕ на реальный ID канала
    },
    {
        username: '@xudobudo_updates',
        name: 'Game Updates', 
        avatar: '🎮',
        id: -1001234567891 // ← ЗАМЕНИТЕ на реальный ID канала
    }
]
```

**Как получить ID канала:**

1. Добавьте бота в канал как администратора
2. Отправьте любое сообщение в канал
3. Используйте метод `getUpdates` для получения ID:
   ```
   https://api.telegram.org/bot8553184364:AAFJbKdlGBeQgpYXw51SoqcfGJ-ezDMt6Oc/getUpdates
   ```
4. В ответе найдите `chat.id` - это и есть ID канала

### 2. Настройка бота

Убедитесь что бот имеет права:
- ✅ Добавлен в каналы как администратор
- ✅ Может читать список участников
- ✅ Токен бота корректный: `8553184364:AAFJbKdlGBeQgpYXw51SoqcfGJ-ezDMt6Oc`

### 3. Настройка параметров

В `script.js` можно настроить:

```javascript
subscriptionState = {
    // Частота проверки подписки (в миллисекундах)
    checkFrequency: 2 * 60 * 1000, // 2 минуты
    
    // Токен бота
    botToken: '8553184364:AAFJbKdlGBeQgpYXw51SoqcfGJ-ezDMt6Oc',
    
    // Список обязательных каналов
    requiredChannels: [...]
}
```

## 🤖 Админ бот для управления каналами

### Установка админ бота

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Настройте админов в `bot-admin.js`:**
   ```javascript
   const ADMIN_IDS = [123456789]; // Ваши Telegram ID
   ```

3. **Запустите бота:**
   ```bash
   npm start
   ```

### Команды админ бота

- `/start` - Приветствие и список команд
- `/channels` - Показать все обязательные каналы
- `/add @username Название` - Добавить канал
- `/remove @username` - Удалить канал
- `/test` - Проверить доступность всех каналов
- `/reset` - Сбросить к каналам по умолчанию
- `/export` - Экспорт конфигурации в JSON

### Пример использования

```
/add @my_channel Мой канал
✅ Канал @my_channel успешно добавлен!

/channels
📋 Обязательные каналы:
1. 📢 XudoBudo News (@xudobudo_news)
2. 🎮 Game Updates (@xudobudo_updates)  
3. 📢 Мой канал (@my_channel)

/remove @my_channel
✅ Канал @my_channel удален из списка
```

## 🎯 Как работает система

### При запуске приложения:
1. Получается ID пользователя из Telegram WebApp
2. Проверяется подписка на все обязательные каналы через Bot API
3. Если не подписан - блокируется весь функционал и показывается модал подписки

### Периодическая проверка:
- Каждые 2 минуты автоматически проверяется статус подписки
- При обнаружении отписки - немедленная блокировка всех функций
- Показывается уведомление о необходимости подписки

### Защищенные функции:
- 🎮 Доступ к играм (Crash)
- 💰 Пополнение баланса  
- 🎁 Получение бонусов и халявы
- 🎯 Промокоды
- 📱 Навигация между разделами

## 🔧 API методы

### Проверка подписки пользователя:
```
POST https://api.telegram.org/bot{BOT_TOKEN}/getChatMember
{
    "chat_id": -1001234567890,
    "user_id": 123456789
}
```

### Возможные статусы участника:
- `creator` - создатель канала ✅
- `administrator` - администратор ✅  
- `member` - участник ✅
- `restricted` - ограничен ❌
- `left` - покинул канал ❌
- `kicked` - заблокирован ❌

## 🎨 UI элементы

### Индикатор статуса (правый верхний угол):
- 🟢 `✅ Подписан` - все подписки активны
- 🔴 `❌ Не подписан` - требуется подписка

### Модал подписки:
- Список всех обязательных каналов
- Кнопки для перехода к подписке
- Статус каждого канала в реальном времени
- Кнопка "Проверить подписки"
- Нельзя закрыть без подписки

## 🧪 Тестирование

### Локальное тестирование:
- Система автоматически определяет localhost/github.io
- Использует демо-режим с симуляцией API
- Случайный статус подписки для тестирования UI

### Продакшн тестирование:
1. Создайте тестовые каналы
2. Добавьте бота как администратора
3. Получите реальные ID каналов
4. Обновите конфигурацию в коде
5. Протестируйте все сценарии:
   - Подписка/отписка
   - Блокировка функций
   - Админ команды

## ⚠️ Важные замечания

1. **ID каналов обязательны** - без них система не работает
2. **Бот должен быть админом** - иначе не сможет проверять участников
3. **CORS ограничения** - для продакшна может потребоваться прокси-сервер
4. **Rate limiting** - Telegram API имеет лимиты на запросы
5. **Приватность** - система работает только с публичными каналами или каналами где бот админ
6. **Нет Grace Period** - при отписке доступ блокируется немедленно

## 🚀 Развертывание

1. Обновите ID каналов в `script.js`
2. Настройте и запустите админ бота
3. Убедитесь что бот добавлен в каналы
4. Протестируйте на GitHub Pages
5. При необходимости настройте прокси для API запросов

Система готова к использованию! 🎉